@model List<KnjigaAutorCRUD.Models.Izdavac>
@{
    ViewData["Title"] = "Autori";
}

<div class="d-flex align-items-center justify-content-between mb-3">
    <h2>Izdavaci</h2>
    <a class="btn btn-success" asp-action="Dodaj">Dodaj</a>
</div>


<table class="table table-dark">
    <thead>
        <tr>
            <th>Sifra</th>
            <th>Ime</th>
            <th>Izmeni</th>
            <th>Obrisi</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var izdavac in Model)
        {
            <tr>
                <td>@izdavac.Id.ToString()</td>
                <td>@izdavac.Ime</td>

                <td>
                    <a class="btn btn-outline-warning" asp-action="Izmeni" asp-route-id="@izdavac.Id">Izmeni</a>
                </td>
                <td>
                    <a class="btn btn-outline-danger" asp-action="Obrisi" asp-route-id="@izdavac.Id">Obrisi</a>
                </td>
            </tr>
        }
    </tbody>
</table>